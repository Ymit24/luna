const cstd = mod {
    const puts = fn@extern["puts"](str: *i8): i32;
    const printf = fn@extern["printf"]@variadic(fmt: *i8): i32;

    const fancy_puts = fn(str: *i8): i32 {
        printf("[FANCY] ");
        return puts(str);
    };

    const deep = mod {
        const stuff: i32 = 100;
    };
};

const main = fn() {
  cstd::puts("Hello world!");
  cstd::printf("stuff: %d\n", cstd::deep::stuff);
  return;
};
