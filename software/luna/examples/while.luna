const puts = fn@extern["puts"](str: *i8)

const main = fn() {
  let val: i32 = 1
  let max: i32 = 5
  puts("preloop")
  while val < max {
    puts("looping..")
    val = 1 + val
  }
  puts("postloop")
}
