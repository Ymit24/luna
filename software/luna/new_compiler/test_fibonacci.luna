const fibonacci = fn(n: i32): i32 {
    if n <= 1 {
        return n;
    } else {
        return fibonacci(n - 1) + fibonacci(n - 2);
    }
};

const main = fn(): i32 {
    let result = fibonacci(10);
    return result;
};